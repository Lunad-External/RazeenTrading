import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { Check, ChevronRight } from "lucide-react";
import { Button } from "@/components/ui/button";
import { ProductCard, WhatsAppButton } from "@/components/catalogue-ui";
import { categories, getProduct, products } from "@/lib/catalogue";
export const Route = createFileRoute("/products/$category/$product")({ loader: ({ params }) => { const product = getProduct(params.product); if (!product || product.category !== params.category) throw notFound(); return product; }, head: ({ loaderData }) => ({ meta: [{ title: loaderData ? `${loaderData.name} | Razeen UAE` : "Product unavailable | Razeen" }, { name: "description", content: loaderData?.description ?? "Product information from Razeen." }, { property: "og:title", content: loaderData ? `${loaderData.name} | Razeen UAE` : "Product unavailable | Razeen" }, { property: "og:description", content: loaderData?.description ?? "Product information from Razeen." }, { property: "og:type", content: "product" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ProductPage });
function ProductPage() { const product = Route.useLoaderData(); const categoryName = categories.find((item) => item.slug === product.category)?.name ?? "Products"; const related = products.filter((item) => item.category === product.category && item.slug !== product.slug).slice(0, 3); return <><section className="site-container py-8"><nav aria-label="Breadcrumb" className="flex flex-wrap items-center gap-2 text-sm text-muted-foreground"><Link to="/products">Products</Link><ChevronRight className="size-4" /><Link to="/products/$category" params={{ category: product.category }}>{categoryName}</Link><ChevronRight className="size-4" /><span>{product.name}</span></nav><div className="mt-8 grid gap-10 lg:grid-cols-2 lg:items-center"><div className="aspect-[4/3] overflow-hidden rounded-xl bg-surface"><img src={product.image} alt={product.name} width={1000} height={800} className="h-full w-full object-cover" /></div><div><p className="text-xs font-bold uppercase text-primary-light">{product.brand}</p><h1 className="mt-3 text-4xl font-extrabold text-primary md:text-5xl">{product.name}</h1><p className="mt-5 text-lg leading-8 text-muted-foreground">{product.description}</p><p className="mt-6 text-sm font-semibold text-muted-foreground">Pricing is available by enquiry.</p><div className="mt-7 flex flex-col gap-3 sm:flex-row"><Button asChild size="lg"><Link to="/quote" search={{ product: product.name }}>Request a Quote</Link></Button><WhatsAppButton /></div></div></div></section><section className="section-space bg-surface"><div className="site-container grid gap-10 lg:grid-cols-2"><div><h2 className="text-2xl font-bold text-primary">Key specifications</h2><ul className="mt-6 grid gap-4">{product.specs.map((spec) => <li key={spec} className="flex gap-3"><Check className="mt-0.5 size-5 shrink-0 text-primary-light" /><span>{spec}</span></li>)}</ul></div><div><h2 className="text-2xl font-bold text-primary">Applications</h2><p className="mt-6 leading-7 text-muted-foreground">Suitable for professional construction, installation, repair and maintenance requirements. Confirm project-specific suitability with the Razeen team when requesting a quote.</p></div></div></section>{related.length > 0 && <section className="section-space"><div className="site-container"><h2 className="text-3xl font-bold text-primary">Related products</h2><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-3 md:gap-6">{related.map((item) => <ProductCard key={item.slug} product={item} />)}</div></div></section>}</>; }