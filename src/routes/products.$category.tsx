import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ChevronRight } from "lucide-react";
import { PageIntro, ProductCard } from "@/components/catalogue-ui";
import { getCategory, products } from "@/lib/catalogue";
export const Route = createFileRoute("/products/$category")({ loader: ({ params }) => { const category = getCategory(params.category); if (!category) throw notFound(); return category; }, head: ({ loaderData }) => ({ meta: [{ title: loaderData ? `${loaderData.name} | Razeen UAE` : "Category unavailable | Razeen" }, { name: "description", content: loaderData?.description ?? "Browse Razeen product categories." }, { property: "og:title", content: loaderData ? `${loaderData.name} | Razeen UAE` : "Category unavailable | Razeen" }, { property: "og:description", content: loaderData?.description ?? "Browse Razeen product categories." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: CategoryPage });
function CategoryPage() { const category = Route.useLoaderData(); const matches = products.filter((product) => product.category === category.slug); return <><PageIntro eyebrow="Product category" title={category.name} copy={category.description} /><section className="section-space"><div className="site-container"><nav aria-label="Breadcrumb" className="mb-8 flex items-center gap-2 text-sm text-muted-foreground"><Link to="/products">Products</Link><ChevronRight className="size-4" /><span>{category.name}</span></nav>{matches.length ? <div className="grid grid-cols-2 gap-4 md:grid-cols-3 md:gap-6 lg:grid-cols-4">{matches.map((product) => <ProductCard key={product.slug} product={product} />)}</div> : <div className="rounded-xl border border-border bg-surface p-10"><h2 className="text-xl font-bold text-primary">Tell us what you need</h2><p className="mt-2 text-muted-foreground">The full {category.name.toLowerCase()} range is being prepared. Send an enquiry for specific products.</p><Link to="/quote" className="mt-5 inline-block font-bold text-primary-light">Request this category →</Link></div>}</div></section></>; }