import { createFileRoute } from "@tanstack/react-router";
import { Check } from "lucide-react";
import { PageIntro, QuoteForm } from "@/components/catalogue-ui";
export const Route = createFileRoute("/quote")({ validateSearch: (search: Record<string, unknown>) => ({ product: typeof search.product === "string" ? search.product : "" }), head: () => ({ meta: [{ title: "Request a Quote | Razeen Building Materials" }, { name: "description", content: "Request a building materials or tools quotation and attach your bill of quantities." }, { property: "og:title", content: "Request a Quote | Razeen Building Materials" }, { property: "og:description", content: "Share your UAE project requirements or BOQ with Razeen." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: QuotePage });
function QuotePage() { const { product } = Route.useSearch(); return <><PageIntro eyebrow="Request a quote" title="Tell us what your project needs" copy="Share a product requirement or upload a bill of quantities. This preview does not yet send or store submissions." /><section className="section-space"><div className="site-container grid gap-12 lg:grid-cols-[0.65fr_1.35fr]"><aside><h2 className="text-2xl font-bold text-primary">A simple enquiry process</h2><ol className="mt-6 grid gap-5">{["Describe the products or project requirement", "Add quantities and your contact details", "Attach a BOQ if you have one", "Submit for sales follow-up"].map((item) => <li key={item} className="flex gap-3"><span className="grid size-6 shrink-0 place-items-center rounded-full bg-secondary text-primary"><Check className="size-4" /></span><span className="text-sm leading-6">{item}</span></li>)}</ol></aside><div className="rounded-xl border border-border p-6 md:p-8"><QuoteForm initialProduct={product} /></div></div></section></>; }